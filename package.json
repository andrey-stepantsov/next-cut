{
  "name": "next-cut",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --outDir dist",
    "test": "jest --coverage",
    "clean": "rm -rf dist node_modules .cache"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.0.5",
    "typescript": "^5.2.2"
  }
}
